import junit.framework.Assert;
import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class NewYm {
        public static String YM = "https://market.yandex.ru/?";
        public static String ID = "//input[@name='login']";

    @Test
    public void yandex() throws InterruptedException {
     //WebDriver driver = new ChromeDriver();
 // driver.get(YM);
   //WebElement btnLogIn = driver.findElement(By.xpath("//a[span=\"Войти\"]"));
 //Assert.assertTrue(btnLogIn.isDisplayed());
  //  btnLogIn.click();
   //   driver.close();
  // driver.findElement(By.xpath("//a[span=\"Войти\"]")).click();
   // Assert.assertTrue(By.xpath("//span[@class='passp-add-account-page-title']")).getText();
     //Assert.assertEquals(ID);

    }
}
